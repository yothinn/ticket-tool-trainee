export enum TicketStatus {
    waitApprove = 'wait for approve',
    waitReassign = 'wait for reassign',
    assigned = 'assigned',
    processing = 'processing',
    closed = 'closed',
    unresolved = 'unresolved',
    cancelled = 'cancelled'
}
